# Auto-Scheduler — Availability UX Session (2026-06-11)

## What shipped this session (all UNCOMMITTED — 4 changes, one coherent batch)
1. **Availability submission status + office approval**
   - Migration `20260611000000_add_availability_status.sql` APPLIED TO PROD:
     `employees.availability_status` text default 'none' ('none'|'pending'|'approved')
   - Crew save on /kitchen/availability → status flips to 'pending' (re-edits after
     approval also re-flip to pending)
   - Office RosterPage: color badge per row (gray "No availability" / amber "Pending
     review" / green "Approved"), one-click Approve button on pending rows, header
     count "N availability submissions awaiting review"; realtime updates live
   - Known simplification: one flag per employee, not per-week — revisit if noisy
2. **"Available with no times = open all day" wording** — crew were already using it
   that way and the data/generator semantics already matched; UI text now confirms it.
   Blue `.avail-explainer` info box on crew page + rewritten editor hints.
3. **"No entry" button removed** — STATUS_OPTIONS now just Available/Unavailable; the
   none state (fully open / inherit-normal-week) is reached via a circular × clear
   button (`.avail-day-clear`) on rows with an entry. Same 3 states underneath,
   presented as 2 choices + undo. (Ryan asked "what's the point of no entry" — answer:
   week-override inheritance + undo; solution was hiding it as a button.)
4. **Privacy fix**: removed "On file: <availability_notes>" banner from crew page —
   manager notes must not be crew-visible; also trimmed availability_notes from the
   crew query entirely (not even in network responses) and deleted orphaned CSS.

## Watch out
- A label experiment mid-session ("Open"/"Set Hours") was REVERTED — Ryan's actual
  insight: crew treat Available+no-times as open-anytime, so confirm that instead.
  Labels are: Available / Unavailable (+ × clear).
- Commit pending: Ryan was told 4 changes are ready to commit as one batch — next
  session should offer commit early. Push still triggers Vercel prod deploy; Ryan only.

## Where things stand
- Phase 1 + kitchen sidebar: committed e6f8d2a (2026-06-10), NOT pushed.
- This session's 4 availability-UX changes: built, prod-migrated, CHANGES.md logged,
  NOT committed.
- Phase 2 (coverage template): fully decided, awaiting Ryan's /sc:implement —
  see `mem:auto-scheduler/session-2026-06-10` for decisions + scope.
