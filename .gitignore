# Secrets & credentials
secrets.txt
supabasepassword/
*.key
*.pem
.env
.env.*
!.env.example

# Dependencies
node_modules/

# Build output
dist/
.vercel/

# Supabase local
supabase/.branches/
supabase/.temp/

# OS
.DS_Store
Thumbs.db

# Git worktrees
.worktrees/

# External framework clone (separate repo, not part of DailyBrief)
SuperClaude_Framework/

# Codex / ChatGPT tooling — local only, never part of the repo.
# AGENTS.md is Codex's converted copy of CLAUDE.md and is deliberately kept on
# disk: it carries the "advisory only, make no changes" instruction. Codex
# regenerates all of this, so it stays ignored rather than tracked.
AGENTS.md
.codex/
.agents/skills/

# Real BEO packets kept on disk for parser reference. Never committed — they
# carry member names, home addresses, phone numbers, emails and member numbers.
BEOs/
